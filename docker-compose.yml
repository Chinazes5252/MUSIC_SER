version: '3.8'

networks:
  dev:

services:
  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile  # <-- Вложен корректно
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./certs:/etc/nginx/certs
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - backend
      - frontend
    networks:
      - dev

  backend:
    build:
      context: ./backend
    env_file:
      - .env
    networks:
      - dev

  frontend:
    build:
      context: ./frontend
    env_file:
      - .env
    networks:
      - dev
